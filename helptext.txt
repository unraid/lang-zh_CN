:main_array_devices_help:
**有色状态指示器** 颜色指示器在 *阵列设备* 中每行开头的含义如下：

<i class='fa fa-circle orb green-orb'></i>正常运行，设备处于活动状态。

<i class='fa fa-circle orb grey-orb'></i>设备处于待机模式（快速启动）。

<i class='fa fa-warning orb yellow-orb'></i>模拟设备内容。

<i class='fa fa-times orb red-orb'></i>设备已禁用，内容已模拟。

<i class='fa fa-square orb blue-orb'></i>新设备。

<i class='fa fa-square orb grey-orb'></i>设备不存在，位置为空。

**标识** 是唯一标识存储设备的 *签名* 。签名
包括设备型号、序列号、Linux设备ID和设备大小。

**温度** (温度) 直接从设备读取。您可以在 [显示首选项](Settings/DisplaySettings)
页面上配置要使用的单位。 我们不读取快速旋转的硬盘驱动器的温度，
因为这通常会使它们旋转，而是显示*符号。我们还会显示 `*` SSD和闪存设备的符号，
尽管有时这些设备确实报告了有效的温度，
有时它们返回值0。

**尺寸, 已用, 剩余** 报告文件的总设备大小、已用空间和剩余空间。这些
还可以在 [显示首选项](Settings/DisplaySettings) 页面上配置单位。 这个
由于文件系统开销，即使对于空磁盘，使用的空间量也将不为零。

*注意：对于多设备缓存池，此数据是btrfs返回的整个池的数据。*

**读取, 写入**是一个发送到设备I/O驱动程序到I/O请求的计数。 这些统计数据可能
随时清除，请参阅下面的阵列状态部分。

**错误数** 统计设备I/O驱动程序报错的 *不可恢复* 的错误数。
使用奇偶校验动态填充因不可恢复的阵列读取错误而丢失的数据。
重建(并且我们尝试将该数据写回失败的扇区)。 任何不可恢复
的写入错误都会导致 *禁用* 磁盘。

**FS** 指的是在设备的分区1中检测到的文件系统类型。

**查看** 包含一个文件夹图标，表示设备已 *挂载*。 单击图标
浏览文件系统。

如果 "显示阵列总计" 在 [显示首选项](Settings/DisplaySettings) 页面是打开的, 则
一个 **总计** 行它提供设备统计数据，包括设备的
平均温度。

必须停止阵列才能更改阵列设备分配。

UnRAID阵列由一个或两个奇偶校验磁盘和多个数据磁盘组成。数据。
磁盘专门用于存储用户数据，奇偶校验磁盘提供必要的冗余
从磁盘故障中恢复。

由于数据不跨阵列条带化，因此奇偶校验磁盘必须与最大数据相同或大于最大数据
磁盘。奇偶校验也应该是您的最高性能驱动器。

每个数据磁盘都有自己的文件系统，并且可以导出为
分开分享。

单击设备名称以配置各个设备设置并启动某些实用程序。
:end

:main_slots_help:
**插槽** 选择服务器中为阵列设备指定的设备插槽数。
阵列插槽的最小数量为2，并且必须至少为该阵列分配一个设备。
:end

:cache_devices_help:
**颜色状态指示器** 在*池设备*中每行开头颜色指示器的意义如下：

<i class='fa fa-circle orb green-orb'></i>正常运行，设备处于可用状态。

<i class='fa fa-circle orb grey-orb'></i>设备处于待机模式（旋转降速）。

<i class='fa fa-square orb blue-orb'></i>新设备。

<i class='fa fa-square orb grey-orb'></i>没有设备，位置为空。

**设备池**是单个设备，或多个设备的池，位于 Unraid 阵列的外部。 可以像阵列设备一样
将其导出以进行网络访问。在 Unraid 阵列之外可以显着加快写入访问速度。

有两种配置池设备的方法：

1. 作为单个设备，或
2. 作为多设备池。

当配置为单个设备时，您可以使用任何受支持的文件系统（ btrfs、reiserfs、
或 xfs ）格式化该设备。 此配置可提供最高的性能，但不以数据保护为代价 -
如果单个池设备发生故障，其中包含的所有数据可能会丢失。

当配置为多设备池时， Unraid OS 将自动选择 *btrfs-raid1* 格式（用于数据
和元数据）。 btrfs 允许将任意数量的设备添加到池中，并且保证
每个数据副本都可以写入两个不同的设备。 因此该池可以承受单磁盘故障而不会丢失数据。

启用［用户共享］ (/Settings/ShareSettings) 时，可以将用户共享配置为
自动使用池设备，以加快写入速度。
可以计划一个称为 *mover* 的特殊后台进程
定期运行，以将用户共享文件从缓存移到阵列上。
:end

:cache_slots_help:
**插槽** 选择服务器中为缓存设备指定的设备插槽数。
:end

:boot_device_help:
重要的阵列配置会保留在 USB 闪存设备上；因此， 必须将其
保持插入服务器。单击［闪存］ (/Main/Flash?name=flash) 以查看 GUID 和
注册信息，并配置导出设置。由于 USB 闪存设备是使用 FAT 文件系统格式化，
因此只能使用 SMB 协议将其导出。
:end

:array_status_help:
**颜色状态指示器** 在*阵列*中的颜色指示器的意义如下：

<i class='fa fa-circle orb green-orb'></i>阵列已启动且奇偶校验有效。

<i class='fa fa-circle orb grey-orb'></i>阵列已停止，奇偶校验有效。

<i class='fa fa-warning orb yellow-orb'></i>阵列已启动，但奇偶校验无效。

<i class='fa fa-warning orb grey-orb'></i>阵列已停止，奇偶校验无效。

:end

:array_devices_help:
#### 分配设备

一个 unRAID 磁盘阵列由一个奇偶校验磁盘和多个数据磁盘组成。 数据磁盘
专门用于存储用户数据，而奇偶校验磁盘提供必要的冗余，
以从任何单个磁盘故障中恢复。

请注意，在引用阵列存储设备时，我们会谨慎使用术语*磁盘*。
当引用实际硬盘驱动器 (HDD) 设备时，我们会使用术语*硬盘驱动器*（有时也称为*驱动器*）。
这是因为在 RAID 系统中，可以读取/写入的阵列磁盘的相应
硬盘驱动器可能已禁用甚至已丢失! 另外，懂得询问 “将哪个设备
分配为奇偶校验磁盘？” 或 “哪个设备对应于 disk2 ？”都会是很有用的

因此我们需要一种方法将硬盘驱动器分配给阵列磁盘。当阵列停止时，
可以在主页上完成此操作。每个阵列磁盘都有一个下拉框，其中列出了所有
未分配设备。T要分配设备，只需从列表中选择它即可。 每次进行设备
分配时，系统都会更新配置文件以记录分配。

#### 要求

与在所有阵列设备上对数据进行条带化的传统 RAID 系统不同， unRAID 服务器
会将文件存储在各个硬盘驱动器上。 因此，所有文件写入操作都将涉及
所写入的数据磁盘和奇偶校验磁盘。原因如下，

* 奇偶校验磁盘的大小必须大于或等于任何数据磁盘的大小，

以及

* 可选择的情况下，奇偶校验磁盘应该要是集合中最快的磁盘。

#### 指导方针

在设计 unRAID 磁盘阵列时，应遵循以下步骤：

1. 确定要用于奇偶校验的硬盘驱动器，以及要用于
数据的硬盘 disk1 、 disk2 等，并以某种方式标记它们。另外，找到每个硬盘驱动器
的序列号并将其记在某个位置； 您稍后将需要此信息。

2. 安装硬盘设备，启动 unRAID 服务器并启动 webGui 。 如果这是全新的
系统构建，主页将显示未安装任何磁盘。这并不意味着系统无法检测
到硬盘；这只是意味着还没有已分配的磁盘。

3. 还记得在步骤 1 中记录的序列号吗？ 对于奇偶校验和每个数据磁盘，
请从下拉列表中根据其序列号选择合适的硬盘驱动器。

#### 热插拔

如果硬件支持，也可以将硬盘驱动器*热插拔*到服务器中。例如，
如果使用的是硬盘驱动器固定框架，可以在通电且阵列已停止的情况下
将它们简单地插入服务器。刷新主页以使新的未分配设备出现在可分配
下拉列表中。

#### 下一步

分配所有硬盘驱动器后，请参考下面的阵列状态部分
并启动阵列。
:end

:encryption_help:
#### 加密输入

当阵列已停止，用户可以指定一个新的加密密钥。 请注意，当使用
特定密钥格式化设备后，只能使用相同的密钥将其打开。更改加密密钥会要求
重新格式化已加密设备 **导致这些设备上所有现有数据永久丢失。**

#### 密码

输入最多512个字符的密码。 强烈建议仅使用 [ASCII表] 强烈建议仅使用 [ASCII 表] (https://en.wikipedia.org/wiki/ASCII)
前128个字符中的95个可打印字符，因为它们始终具有相同的二进制表示形式。
根据系统配置，其他字符可能具有不同的编码， 您的密码
将无法使用其他编码。如果您希望使用更长的密码或包含二进制数据，请上载密钥文件。

请参考 [cryptsetup FAQ] (https://gitlab.com/cryptsetup/cryptsetup/wikis/FrequentlyAskedQuestions#5-security-aspects)
以认识构成*安全*密码的元素。

**记住**这个密码。 **如果丢失密码，已加密的内容将无法恢复！**

#### 密钥文件

选择具有存储的加密密钥的本地密钥文件或二进制文件.密钥文件的最大大小为8M（8388608字节）.

**备份** 您的本地密钥文件. **如果丢失了,加密的内容将无法恢复！**
:end

:info_warning_temp_help:
*警告磁盘温度* 设置此硬盘温度的警告阈值,超过此阈值将导致警告通知.

零值将禁用警告阈值（包括通知）.
:end

:info_critical_temp_help:
*磁盘临界温度* 设置此硬盘温度的临界阈值.超过此阈值将导致警报通知.

零值将禁用关键阈值（包括通知）.
:end

:info_file_system_help:
输入所需的文件系统类型,更改设备的文件系统类型将允许您重新格式化
使用新文件系统的设备.请注意 **设备上的所有现有数据都将丢失**.
:end

:info_comments_help:
该文本将显示在Windows资源管理器中共享的 “注释” 列下
输入您喜欢的任何字符,最多256个字符.
:end

:info_warning_utilization_help:
*警告磁盘利用率* 设置此硬盘利用率的警告阈值,超过此阈值将导致警告通知.

当警告阈值设置为等于或大于严重阈值时,将仅存在严重通知（不存在警告）,

零值将禁用警告阈值（包括通知）.
:end

:info_critical_utilization_help:
*关键磁盘利用率* 设置此硬盘利用率的关键阈值,超过此阈值将导致警报通知,

零值将禁用关键阈值（包括通知）.
:end

:info_btrfs_balance_help:
** 余额 ** 将运行 *btrfs余额* 程序以重新整理所有池设备中的盘区;例如,
将池从raid1转换为raid0或反之亦然.

当实现了 *完全余额* 时,它基本上会重写当前文件系统中的所有内容.

*模式转换*会影响btrfs数据区；元数据始终使用raid1,并在必要时通过任何平衡操作转换为raid1.

运行时间可能非常长,具体取决于设备的文件系统大小和速度.

UnRAID在创建多设备池时使用以下默认选项：

`-dconvert=raid1 -mconvert=raid1`

有关更完整的文档,请参阅btrfs余额 [手册页](https://btrfs.wiki.kernel.org/index.php/Manpage/btrfs-balance)

*注：普遍认为raid5和raid6仍是 **实验性** 在linux社区*.
:end

:info_balance_cancel_help:
**取消** 将取消进行中的余额操作.
:end

:info_btrfs_scrub_help:
**擦洗** 运行 *btrfs擦洗* 程序,它将从所有程序读取所有数据和元数据块
设备并验证校验和.

如果选中了*修复损坏的块*,且有正确的副本可用,*btrfs擦洗* 将修复损坏的块.
:end

:info_scrub_cancel_help:
**取消** 将取消正在进行的清理操作.
:end

:info_btrfs_check_help:
**检查** 将运行 *btrfs检查* 程序以检查设备上的文件系统完整性.

*选项* 字段初始化为 *--只读* ,指定只读.如果需要修复,您应该运行
第二次检查,将 *选项* 设置为 *--修复* ；这将允许 *btrfs检查* 修复文件系统.

开始检查后,您应该刷新以监视进度和状态.取决于
文件系统有多大,可能会出现什么错误,因此该操作可能需要 **很长时间** （几小时）才能完成.
窗口中没有打印太多信息,但是您可以通过观察读写计数器来验证操作是否正在运行
在主页上增加设备.
:end

:info_check_cancel_help:
**取消** 将取消正在进行的检查操作.
:end

:info_reiserfs_check_help:
**检查** 将运行 *reiserfsck* 程序检查设备上的文件系统完整性.

*选项* 字段可能填入用于修复文件系统中问题的特定选项.通常情况下,您
首先运行一次检查,将*选项*留空.完成后,如果*reiserfsck*发现任何问题,您必须
使用第一次 *reiserfsck* 检查所指示的特定选项,运行第二次检查次.

开始检查后,您应该刷新以监视进度和状态.取决于
文件系统有多大,可能会出现什么错误,因此该操作可能需要 **很长时间** （几小时）才能完成.
窗口中打印的信息不多,但您可以通过观察读/写计数器来验证操作是否正在运行
在主页上增加设备.
:end

:info_reiserfs_cancel_help:
**取消** 将取消正在进行的检查操作.
:end

:info_xfs_check_help:
**检查** 将运行 *XFS_修复* 程序以检查设备上的文件系统完整性.

*选项* 字段使用指定仅检查的 *-n* 进行初始化.如果需要修复,您应该运行
第二次检查,将 *选项* 设置为空；这将允许 *XFS_修复* 修复文件系统.

开始检查后,您应该刷新以监视进度和状态.取决于
文件系统有多大,可能会出现什么错误,因此该操作可能需要 **很长时间** （几小时）才能完成.
窗口中没有打印太多信息,但是您可以通过观察读写计数器来验证操作是否正在运行
在主页上增加设备.
:end

:info_xfs_cancel_help:
**取消** 将取消正在进行的检查操作.
:end

:info_smart_notifications_help:
根据属性的原始RAW值增加或达到制造商设置的预定义阈值的降低的NORMALIZED值,生成SMART通知.

每个磁盘可能都有自己的特定设置,而不是 '默认' 设置（请参阅磁盘设置下的全局SMART设置）.
:end

:info_tolerance_level_help:
可以给出一个公差级别,以防止微小的变化导致通知.设置的公差级别过高可能会导致重大更改,而不会发出通知.

每个磁盘可能都有自己的特定设置,而不是 '默认' 设置（请参阅磁盘设置下的全局SMART设置）.
:end

:info_controller_type_help:
默认情况下,smartctl会自动选择控制器,以读取SMART信息;但是,某些控制器需要特定的设置才能使smartctl正常工作.
使用此设置选择您的控制器类型,并根据您的情况填写特定的磁盘索引和设备名称.使用制造商的文档来查找相关信息.

每个磁盘可能都有自己的特定设置,而不是 '默认' 设置（请参阅磁盘设置下的全局SMART设置）.
:end

:info_attribute_notifications_help:
用户可以为给定的SMART属性启用或禁用通知.建议保留默认值,即所有选定的属性,
如果您的硬盘上没有某些属性或提供的信息不正确,则可能会排除这些属性.
另外,可以输入自定义SMART属性以生成通知.请谨慎选择
如果选择了不适当的SMART属性,可能会导致大量通知.

每个磁盘可能都有自己的特定设置,而不是 '默认' 设置（请参阅磁盘设置下的全局SMART设置）.
:end

:selftest_history_help:
按 **显示**查看保存在磁盘上的自检历史记录.
仅当磁盘处于活动模式时,此功能才可用.
:end

:selftest_error_log_help:
按 **显示** 查看错误报告,该报告保留在磁盘本身上.
仅当磁盘处于活动模式时,此功能才可用.
:end

:selftest_short_test_help:
启动 *短的* 智能自检,预计持续时间可以在 *功能* 部分下查看,这通常需要几分钟.

旋转磁盘后,它将中止所有正在运行的自检.
仅当磁盘处于活动模式时,此功能才可用.
:end

:selftest_long_test_help:
启动 *扩展* 智能自检,预计持续时间可在 *功能* 部分下查看.这通常需要几个小时.

旋转磁盘后,它将中止所有正在运行的自检.建议禁用磁盘的降速定时器
以避免中断此自检.

仅当磁盘处于活动模式时,此功能才可用.
:end

:selftest_result_help:
如果没有运行任何测试,它将在此处显示最新获得的自测结果（如果有）.
否则,将显示正在运行的测试的进度指示器（百分比值）.
:end

:smart_attributes_help:
此列表显示此磁盘支持的SMART属性.有关每个SMART属性的更多信息,建议在线搜索.

*橙色* 中的属性可能需要您注意.它们的 **原始值** 大于零,可能表示磁盘挂起故障.

当特定属性原始值开始随时间增加时,需要特别注意.如有疑问,请咨询Limetech论坛以获取建议.
:end

:smart_capabilities_help:
此列表显示此磁盘支持的SMART功能.

在此处观察SMART短期和扩展自我测试的估计持续时间.
:end

:smart_identity_help:
此列表显示此磁盘的SMART身份信息
:end

:open_devices_help:
这些是服务器中安装的设备,但未分配给受奇偶校验保护的设备
阵列或缓存磁盘/池.
:end

:flash_backup_help:
使用 *闪存备份* 创建闪存设备当前内容的单个zip文件,并将其本地存储在您的计算机上.
:end

:syslinux_cfg_help:
使用此页面可以更改您的 `syslinux.cfg` 文件.
您将需要重新引导服务器,以使这些更改生效.
:end

:syslinux_button_help:
点击 **默认值** 按钮,以使用
出厂默认内容.您仍然需要点击 **应用** 才能
提交更改.

单击 **应用** 按钮,提交本次编辑.单击 **重置** 以
撤消您所做的任何更改(在保存之前),单击 **完成** 退出此页面.
:end

:info_free_space_help:
这为卷中剩余的空闲空间量定义了一个“底限”.
如果可用空间小于此值,则通过用户共享写入的新文件将失败,并显示 "没有足够的空间" 错误.

输入带有以下后缀之一的数值：

**KB** = 1,000<br>
**MB** = 1,000,000<br>
**GB** = 1,000,000,000<br>
**TB** = 1,000,000,000,000<br>
**K** = 1,024 (ie, 2^10)<br>
**M** = 1,048,576 (ie, 2^20)<br>
**G** = 1,073,741,824 (ie, 2^30)<br>
**T** = 1,099,511,627,776 (ie, 2^40)<br>

如果没有后缀,则假定为1024个字节的块数.
:end

:share_list_help:
**彩色状态指示器** -- *用户共享* 中每行开头的颜色指示器的意义如下：

<i class='fa fa-circle orb green-orb'></i>所有文件都位于受保护的存储区中.

<i class='fa fa-warning orb yellow-orb'></i>某些或所有文件都处于不受保护的存储中.

**安全模式:**

+ '-' -- 用户共享未导出且在网络上不可用
+ *公共* -- 包括来宾在内的所有用户都具有完全读/写访问权限(开放访问)
+ *安全* -- 包括来宾在内的所有用户都有读访问权限，写访问权限是按用户设置的(受限访问权限)
+ *私人* -- 根本没有来宾访问权限，每个用户设置读/写或只读访问权限(关闭访问)

**特殊模式:**

+ SMB 以*斜体*显示的安全模式表示导出的隐藏用户共享。
+ AFP 以*斜体*显示的安全模式表示导出的时光机用户共享。
+ NFS 没有针对用户共享的特殊模式。
:end

:disk_list_help:
**彩色状态指示器** -- *磁盘共享* 中每行开头的颜色指示器的含义如下：

<i class='fa fa-circle orb green-orb'></i>已安装的基础设备具有冗余/保护功能.

<i class='fa fa-warning orb yellow-orb'></i>已安装的基础设备没有冗余/保护.

**安全模式:**

+ '-' -- 磁盘共享未导出且在网络上不可用
+ *公共* -- 包括来宾在内的所有用户都具有完全读/写访问权限(开放访问)
+ *安全* -- 包括来宾在内的所有用户都有读访问权限，写访问权限是按用户设置的(受限访问权限)
+ *私人* -- 根本没有来宾访问权限，每个用户设置读/写或只读访问权限(关闭访问)

**特殊模式:**

+ SMB 以*斜体*显示的安全模式表示导出的隐藏用户共享。
+ AFP 以*斜体*显示的安全模式表示导出的时光机用户共享。
+ NFS 没有针对用户共享的特殊模式。
:end

:share_edit_global1_help:
*共享*,也称为 *用户共享* ,只是存在于一个或多个
存储设备（阵列和缓存）.每个共享都可以导出以进行网络访问.浏览共享时,我们返回
每个存储设备上都有该顶级目录的所有文件和子目录的组合视图.

*读取设置* 用于将新共享的设置预置为现有共享的设置.

选择所需的共享名称,然后按 **读取** 从所选源复制设置.
:end

:share_edit_global2_help:
*将设置写入* 用于将当前共享的设置复制到一个或多个其他现有共享.

选择所需的目的地,然后按 **写入** 将设置复制到选定的共享.
:end

:share_edit_name_help:
共享名最多可以包含40个字符,并且区分大小写,并具有以下限制：

* 不能包含双引号字符(“)或以下字符：/ \* < > |
* 不能是保留的共享名称之一：flash、cache、cache2、..、disk1、disk2、..

强烈建议您简化生活并避免使用特殊字符.
:end

:share_edit_comments_help:
您喜欢的任何字符,最多256个字符.
:end

:share_edit_allocation_method_help:
此设置确定UnRAID操作系统在创建新文件或目录时如何选择要使用的磁盘：

**高水位**
选择可用空间仍高于当前 *高水位线* 的编号最低的磁盘.
*高水位线*用最大的数据盘大小除以2进行初始化.  如果没有磁盘
的可用空间超过当前的 *高水位线*,请将 *高水位线* 除以2,然后重新选择.

**高水位** 的目标是向每个磁盘写入尽可能多的数据(以便最小化
磁盘需要旋转的频率),同时尝试保持相同数量的可用空间量
每个磁盘(以便跨阵列均匀分布数据).

**填补**
选择编号最小的磁盘,其剩余可用空间仍大于当前的**最小可用空间**
设置.

**最自由**
选择当前具有最大可用空间的磁盘.
:end

:share_edit_free_space_help:
*最小可用空间* 可用于写入属于该共享的任何磁盘.<br>

选择一个等于或大于您打算复制到共享的最大单个文件大小的值.
适当包含单位KB,MB,GB和TB,例如10MB.
:end

:share_edit_split_level_help:
确定是否允许目录扩展到多个磁盘.

**根据需要自动拆分任何目录**
当需要在共享中创建新文件或子目录时,Unraid OS首先选择哪个磁盘
它应该根据配置的 *分配方法* 创建.如果父目录包含
该磁盘上不存在新文件或子目录,则Unraid OS将首先创建所有必需文件
父目录,然后创建新文件或子目录.

**根据需要仅自动拆分顶级目录**
在共享的第一级子目录中创建新文件或子目录时,如果
要写入的磁盘上不存在该第一级子目录,则将首先创建子目录.
如果要在共享的第二级或较低级子目录中创建新文件或子目录,
则会在与新文件或子目录的父目录相同的磁盘上创建新文件或子目录.

**根据需要仅自动拆分前 "N" 级目录**
与以前类似：当创建新文件或子目录时,如果父目录处于"N" 级,
并且在所选磁盘上不存在,则UnRAID OS将首先创建所有必要的父目录.
如果新文件或子目录的父目录超出级别 "N",
则在父目录所在的同一磁盘上创建新文件或子目录.

**手动：不自动拆分目录**
当需要在共享中创建新文件或子目录时,Unraid OS将仅考虑在
父目录已存在.
:end

:share_edit_included_disks_help:
指定共享可以使用的磁盘.默认情况下,所有磁盘都包括在内；也就是说,如果具体
此处未选择磁盘,则共享可能会扩展为 *所有* 阵列磁盘.
:end

:share_edit_excluded_disks_help:
指定共享可以 *不* 使用的磁盘.默认情况下,不排除任何磁盘.
:end

:share_edit_cache_pool_help:
指定是否可以将写入共享的新文件和目录写入缓存磁盘/池(如果存在).
此设置还会影响 *移动* 行为.

**否**禁止将新文件和子目录写入缓存磁盘/池.
*移动*不会执行任何操作,因此缓存中此共享的所有现有文件都会保留在那里.

**是**表示所有新的文件和子目录都应该写入到高速缓存磁盘/池中,前提
是高速缓存磁盘/池上有足够的可用空间.
如果缓存磁盘/池上的空间不足,则会在阵列上创建新文件和目录.
调用 *移动* 时,文件和子目录将从缓存磁盘/池传输到阵列.

**仅仅**表示所有新文件和子目录都必须写入高速缓存磁盘/池.
如果缓存磁盘/池上没有足够的可用空间,*创建* 操作将失败,并显示 *空间不足* 状态.
*移动*不会执行任何操作,因此缓存中此共享的所有现有文件都会保留在那里.

**首选**表示所有新文件和子目录都应该写入高速缓存磁盘/池,前提
是高速缓存磁盘/池有足够的空闲空间.
如果缓存磁盘/池上的空间不足,则会在阵列上创建新文件和目录.
调用 *移动* 时,文件和子目录将从阵列传输到缓存磁盘/池.

**注意：**移动不会移动当前正在使用的任何文件.
这意味着,如果您要移动与Docker或VM等系统服务关联的文件,则
需要在Mover运行时禁用这些服务.
:end

:share_edit_copy_on_write_help:
设置为 **否** 将导致在使用 *btrfs* 文件系统格式化的设备上创建共享目
录时,在共享目录上设置 *btrfs* NOCOW(写入时无拷贝)属性.设置
后,设备上新创建的文件和子目录将继承NOCOW属性.我们建议将此
设置用于存储虚盘映像的共享,包括Docker环回映像文件.此设置对
非btrfs文件系统没有影响.

设为**自动**即可正常运行,这意味着COW **将** 在 * btrfs *格式化的设备上生效.
:end

:share_edit_status_help:
共享 *不* 包含任何数据,如果不再需要,可以将其删除.
:end

:share_edit_delete_help:
只要共享包含数据,就不能删除它.请注意,某些数据可能会被隐藏.另请参阅 [SMB设置](/Settings/SMB) -> 隐藏 "点" 文件
:end

:smb_security_help:
*读取设置来源* 用于将当前所选共享的SMB安全设置预置为现有共享的设置.

选择所需的共享名称,然后按**读取**,从所选源复制SMB安全设置.

*将设置写入*用于将当前选定共享的SMB安全设置复制到一个或多个其他现有共享.

选择所需目标,然后按 **写入** 将SMB安全设置复制到所选共享.
:end

:smb_export_help:
此设置确定共享是否可见和/或可访问.'是(隐藏)' 设置
将向*浏览*隐藏共享,但如果您知道共享名称,则仍可访问.
:end

:smb_time_machine_volume_help:
这限制了报告的卷大小,从而阻止Time Machine使用整个实际磁盘空间
用于备份.例如,将此值设置为“ 1024”会将报告的磁盘空间限制为1GB.
:end

:smb_case_sensitive_names_help:
控制文件名是否区分大小写.

默认设置 **自动** 允许支持区分大小写的文件名(Linux CIFSVFS)的客
户端按数据包告知Samba服务器希望以区分大小写的方式访问文件系统
(以支持UNIX区分大小写的语义). 没有Windows系统支持区分大小写的文件名,因此将此选项
设置为 **自动** 等同于将其设置为No；但是,将保留Windows客户端传递的文件名的大小写. 此设置可能会降低
非常大目录的性能,因为Samba必须对传递的名称执行文件名搜索和匹配.

设置为  **是**表示文件是在客户端通过的情况下创建的,只有在相同的情况下才能访问.
这将加快非常大的目录访问速度,但某些Windows应用程序可能无法在此设置下正常运行.
例如,如果创建了"MyFile",但Windows应用程序试图打开 "MYFILE"(这在Windows中是允许的),则将找不到它.

**强制降低** 的值是特殊的：所有传入的客户端文件名的大小写都会设置为小写,而不仅仅是新的文件名.
换句话说,无论在Windows端创建了哪种大小写混合的名称,都将以所有小写形式存储和访问它.这个
确保所有Windows应用程序无论大小写都能正确找到任何文件,但大小写不会保留在文件夹列表中.
请注意,仅应为新共享配置此设置.
:end

:smb_security_modes_help:
安全模式摘要：

**公共** 包括来宾在内的所有用户都具有完全的读取/写入权限.

**安全** 包括来宾在内的所有用户都具有读取权限,您可以选择其中的哪个用户
有写权限.

**私人** 根本没有访客访问权限,您可以选择哪些用户具有读/写权限或
只读访问.
:end

:smb_secure_access_help:
*读取设置* 用于将当前所选共享的SMB用户访问设置预设为现有共享的设置.

选择所需的共享名称,然后按**读取**,从所选源复制SMB安全设置.

*将设置写入*用于将当前共享的SMB用户访问设置复制到一个或多个其他现有共享.

选择所需目标,然后按**写入**将SMB用户访问设置复制到所选共享.
:end

:smb_private_access_help:
*读取设置*用于将当前所选共享的SMB用户访问设置预设为现有共享的设置.

选择所需的共享名称,然后按**读取**,从所选源复制SMB安全设置.

*将设置写入*用于将当前共享的SMB用户访问设置复制到一个或多个其他现有共享.

选择所需目标,然后按**写入**将SMB用户访问设置复制到所选共享.
:end

:nfs_security_help:
*读取设置*用于将当前选定共享的NFS安全设置预置为现有共享的设置.

选择所需的共享名称,然后按**读取**,从所选源复制NFS安全设置.

*将设置写入*用于将当前选定共享的NFS安全设置复制到一个或多个其他现有共享.

选择所需目标,然后按**写入**将NFS安全设置复制到所选共享.
:end

:user_add_username_help:
用户名最长可达40个字符,并且必须以 **小写字母** 或下划线开头,
后跟**小写字母**、数字、下划线或破折号.它们可以以美元符号结束.
:end

:user_add_description_help:
最多64个字符.不允许使用的字符与符号,引号和冒号.
:end

:user_add_custom_image_help:
图像将缩放到48x48像素.图像文件上传的最大大小为95 kB(97,280字节).
:end

:user_password_help:
最多128个字符.
:end

:user_edit_description_help:
最多64个字符.不允许使用的字符与符号,引号和冒号.
:end

:user_edit_custom_image_help:
图像将被缩放为48x48像素.图片文件的最大上传大小为512 kB（524,288字节）.
:end

:cpu_vms_help:
此页面提供了VM的当前CPU固定分配的总视图.<br>
它还允许修改这些分配.

正在运行的虚拟机 **先停止**,修改后重启.<br>
停止的VM会立即被修改,并且启动VM时新的分配将变为活动状态.

当按下 ***应用*** 时,将执行扫描以查找更改,随后并行更改具有更改的容器.

*重要提示：请等到所有更新完成后再离开此页面*.
:end

:cpu_pinning_help:
此页面提供了Docker容器当前CPU固定分配的总视图.<br>
它还允许修改这些分配.

正在运行的容器 ** 先停止**,然后在修改后重新启动.<br>
用户手动启动容器后,立即修改停止的容器,并激活新的分配.

当按下 ***应用*** 时,将执行扫描以查找更改,随后并行更改具有更改的容器.

*重要提示：请等到所有更新完成后再离开此页面*.

默认情况下,没有为Docker容器选择任何核心,这意味着它使用了所有可用的核心.<br>
不要为容器选择 **全部** 核心,如果您希望不受限制地使用核心,请选择 **否** 即可.

不要为 *隔离* 的容器选择核心.
通过设计,当选择多个隔离的核心时,容器将仅使用单个核心（编号最小的核心）.<br>
通常,这不是用户选择多个内核时想要的.
:end

:cpu_isolation_help:
CPU隔离允许用户指定要显式保留用于分配（分配给VM或Docker容器）的CPU核心.

这对于游戏虚拟机平稳运行至关重要,因为即使您手动固定Docker容器使其与游戏虚拟机不重叠,
主机OS仍可以使用与来宾VM所需的内核相同的内核来执行诸如为WebGui返回响应,运行奇偶校验,btrfs操作等操作.
:end

:timezone_help:
从下拉列表中选择您适用的时区.
:end

:use_ntp_help:
选择 '是' 以使用网络时间协议来保持服务器时间准确.
强烈建议您使用网络时间服务器,特别是如果您打算使用Active Directory.

注意：如果使用 `pool.ntp.org` 时间服务器,也请参考[其文档]（http://www.pool.ntp.org/en/use.html）.
:end

:ntp_server1_help:
这是要使用的主要NTP服务器.输入FQDN或IP地址.
如果NTP服务器1关闭,则这是备用NTP服务器.
如果NTP服务器1和2都关闭,则这是备用NTP服务器.
如果NTP服务器1、2和3都关闭,这是备用NTP服务器.
如果NTP服务器1关闭,则这是备用NTP服务器.
:end

:ntp_server3_help:
如果NTP服务器1和2都关闭,则这是备用NTP服务器.
:end

:ntp_server4_help:
如果NTP服务器1、2和3都关闭,这是备用NTP服务器.
:end

:current_time_help:
输入当前时间.使用格式YYYY-MM-DD HH：MM：SS.使用NTP时显示为灰色.
:end

:disk_enable_autostart_help:
如果设置为 '是',则在服务器启动时设备配置正确时,
阵列将自动启动并导出共享.<br>
如果设置为 '否',则必须自己启动阵列.
:end

:disk_spindown_delay_help:
该设置定义 ‘默认’ 的超时，用于在一段没有I/O 活动的时间后
将硬盘驱动器降速，您可以在磁盘设置页面为单独的某个磁盘
替换其默认值。
:end

:disk_spinup_groups_help:
当设置为 ‘是’ 后 [提速群组] (/Help) 特性将启用。
:end

:disk_default_partition_format_help:
定义在格式化**仅 2TB 及以下**容量的硬盘驱动器时
创建的分区布局类型。（所有大于 2TB 的设备都只能设置为 GPT 分区表。）

**MBR ：未对齐**设置会创建 MBR-style 分区表，其中单个分区 1
将会从磁盘开头的 **第63扇区** 开始。这几乎是所有
MBR-style 分区表的*传统*设置。

**MBR ：4K-对齐**设置会创建 MBR-style 分区表，其中单个分区 1
将会从磁盘开头的 **第64扇区** 开始。由于扇区的大小是 512 byte ，
这将使分区 1 以 4K-byte 为边界 *对齐*。对于所谓的 *高级格式化*的磁盘
需要该选项以提供合适的支持。

除非您拥有特定的需求，否则请不要对默认的 **MBR ：4K-对齐** 设置进行更改。
:end

:disk_default_file_system_help:
定义在格式化一个*不可挂载的*阵列设备时，默认创建的文件系统类型。

对于单个或多个设备的缓存，默认的文件系统类型始终为 Btrfs 。
:end

:disk_shutdown_timeout_help:
在关闭服务器时，该选项定义了在进行强制关机之前，等待多少秒
以实现 *优雅* 关闭。
:end

:disk_tunable_poll_attributes_help:
该选项以秒为单位，定义磁盘 SMART 的轮询间隔。设为0 值将会禁用 SMART 轮询（不推荐）。
:end

:disk_tunable_enable_ncq_help:
当设置为 **否** 则会禁用所有支持 NCQ 的阵列设备的 *原生命令排序* 。

**自动**会对每个设备按默认方式设置。

备注：在选择自动后，必须重新启动以使设置生效。
:end

:disk_tunable_nr_requests_help:
该选项为所有的阵列设备定义 'nr_requests' 设备驱动设置。

**自动**会对每个设备按默认方式设置。

备注：当您设置为空白并单击提交，该设置项将还原为默认值，系统必须重新启动以使设置生效。
:end

:disk_tunable_scheduler_help:
为所有的阵列设备选择内核 I/O 调度程序。

**自动**会对每个设备按默认方式设置 (mq-deadline) 。

备注：在选择自动后，必须重新启动以使设置生效。
:end

:disk_tunable_md_num_stripes_help:
该项为 *带区池* 的 *带区* 数的大小。*带区* 指一种便于并行 4K 读/写的数据结构
对奇偶校验保护的阵列，必须配置该选项。

备注：当您设置为空白并单击提交，该设置项将还原为默认值，系统必须重新启动以使设置生效。
:end

:disk_tunable_md_queue_limit_help:
该选项是一个位于 [1..100] 区间内的数，是允许使用的带区池的最大恒定负载百分比。

备注：当您设置为空白并单击提交，该设置项将还原为默认值。
:end

:disk_tunable_md_sync_limit_help:
该选项是一个位于 [0..100] 区间内的数，是分配给奇偶校验同步/检查的带区池剩余状态 I/O 的最大百分比。

备注：当您设置为空白并单击提交，该设置项将还原为默认值。
:end

:disk_tunable_md_write_method_help:
选择写入到奇偶校验保护阵列中的可用磁盘时采用的方式。

设为*自动* 会选择‘读取/修改/写入’。
:end

:disk_default_warning_utilization_help:
*磁盘警告利用率* 为所有的硬盘利用率设置默认的警告阈值。超出这一阈值将会产生警告通知。

当警告阈值设为等于或大于危险阈值时，将只会存在危险通知（不存在警告通知）。

设为 0 值将会禁用警告阈值（包括通知）。
:end

:disk_default_critical_utilization_help:
*磁盘危险利用率* 为所有的硬盘利用率设置默认的危险阈值。超出这一阈值将会产生警报通知。

设为 0 值将会禁用危险阈值（包括通知）。
:end

:disk_default_warning_temperature_help:
*磁盘警告温度* 为所有的硬盘温度设置默认的警告阈值。超出这一阈值将会产生警告通知。

设为 0 值将会禁用警告阈值（包括通知）。
:end

:disk_default_critical_temperature_help:
*磁盘危险温度* 为所有的硬盘温度设置默认的危险阈值。超出这一阈值将会产生警报通知。

设为 0 值将会禁用危险阈值（包括通知）。
:end

:disk_default_smart_notification_help:
当 RAW 属性值增长到，或标准值降低到了制造商设定的阈值时，将会生成 SMART 通知。

这一部分用于对所有磁盘进行全局设置。当然对个别硬盘调整设置也是可行的。
:end

:disk_default_smart_tolerance_help:
容差级别可用于阻止微小更改引发的通知。设置过高的容差级别可能导致对于危险更改毫无预警。

这一部分用于对所有磁盘进行全局设置。当然对个别硬盘调整设置也是可行的。
:end

:disk_default_smart_controller_help:
自动控制器选择默认由 smartctl 完成，以读取 SMART 信息。然而，特定的控制器需要为 smartctl 进行特别设置才可工作。
使用该设置项选择控制器类型，并为您的应用场景填写特定的磁盘索引和设备名。使用制造商的文档以发现相关信息。

这一部分用于对所有磁盘进行全局设置。当然对个别硬盘调整设置也是可行的。
:end

:disk_default_smart_attribute_help:
用户能够启用或禁用给定的 SMART 属性。推荐保持选择所有属性的默认设置，
当您的硬盘不支持某些特定属性，或不提供正确的信息时，则可以禁用它们。
此外，自定义可输入的 SMART 属性能够生成通知。请谨慎选择该项，
如果选中了不合适的 SMART 属性，可能导致通知崩溃。

这一部分用于对所有磁盘进行全局设置。当然对个别硬盘调整设置也是可行的。
:end

:docker_repositories_help:
使用该区域来添加模板存储库。
Docker 模板用于轻松创建和重建 Docker 容器。每行请仅设置一个。

访问该页面以查找社区支持的流行存储库：<a href="http://lime-technology.com/forum/index.php?topic=37958.0" target="_blank">http://lime-technology.com/forum/index.php?topic=37958.0</a>
:end

:docker_enable_help:
在第一次运行 Docker 服务之前，请指定一个用于安装 Docker 的映像文件。

完成首次启动之后， Docker 将总会在阵列启动后自动启动。
:end

:docker_vdisk_size_help:
此处为系统在创建一个新的 docker 映像文件时，以 GB 指定的默认大小。

若要调整一个已有的映像文件的大小，请在此处指定新的值。文件（及文件系统）将会在下一次启动 Docker 服务的时候增大到新的大小（但绝不会减小）。
:end

:docker_vdisk_location_help:
必须为 Docker 指定一个映像文件。系统会在 Docker 服务首次启动时自动创建该文件。

该映像的文件名必须以 .img 为扩展名，否则系统将无法接受该输入并将其标红。

推荐将该映像文件创建在阵列之外，例如在缓存池中。为获得最佳体验， 请首选 SSD 设备。
:end

:docker_appdata_location_help:
您可以指定一个文件夹，用于自动生成和存储包含每个 Docker 应用配置文件的子文件夹（通过 /config 已映射的卷）。

该文件夹的路径必须以一个反斜杠 (/) 字符结尾。否则系统将无法接受该输入并将其标红。

推荐将该映像文件创建在阵列之外，例如在缓存池中。为获得最佳体验， 请首选 SSD 设备。

只在添加新的 Docker 应用时使用。编辑已有的 Docker 应用不会影响该设置。
:end

:docker_log_rotation_help:
默认禁用日志轮换，并创建单个不限大小的日志文件。

启用日志轮换可限制日志文件的大小，并指定保存在轮换方案中的文件数。
:end

:docker_log_file_size_help:
指定最大的日志大小。超出时将会产生日志轮换。
:end

:docker_log_file_number_help:
指定当启用日志轮换时的日志文件数。
:end

:docker_authoring_mode_help:
如果设为 **是** ，在创建/编辑容器时，界面将会呈现一些额外的与模板创作相关的区域。
:end

:docker_custom_network_access_help:
允许主机和容器间利用一个自定义的 (macvlan) 网络直接通信。<br>
默认禁用该选项。
:end

:docker_user_defined_network_help:
用户定义网络是指由用户在 GUI 之外创建的网络。
默认从 Docker 中移除用户定义网络。这么做是为了阻止潜在的与自动生成的自定义网络间的冲突。

更改这一设置以保留用户定义网络，但用户有责任确保这些项正确工作且不会发生冲突。
:end

:docker_include_interface_vlan_ipv4_help:
为 Docker 自定义网络包括（默认值）或排除上述接口或 VLAN。

输入每个被分配的子网的池范围，这将被 Docker 用于 DHCPv4 分发。例如 192.168.1.128/25
:end

:docker_exclude_interface_vlan_ipv4_help:
为 Docker 自定义网络包括（默认值）或排除上述接口或 VLAN。

输入每个被分配的子网的池范围，这将被 Docker 用于 DHCPv4 分发。例如 192.168.1.128/25
:end

:docker_include_interface_vlan_ipv6_help:
为 Docker 自定义网络包括（默认值）或排除上述接口或 VLAN。

输入每个被分配的子网的池范围，这将被 Docker 用于 DHCPv6 分发。例如 2a02:abcd:9ef5:100:1::/72
:end

:docker_exclude_interface_vlan_ipv6_help:
为 Docker 自定义网络包括（默认值）或排除上述接口或 VLAN。

输入每个被分配的子网的池范围，这将被 Docker 用于 DHCPv6 分发。例如 2a02:abcd:9ef5:100:1::/72
:end

:docker_version_help:
使用中的 Docker 版本。
:end

:docker_vdisk_location_active_help:
Docker 映像文件的位置。
:end

:docker_appdata_location_active_help:
Docker 容器的存储位置。
:end

:docker_log_rotation_active_help:
默认创建单个无限制大小的日志文件。否则当启用日志轮换时，日志的大小及文件数将受限。
:end

:docker_custom_network_active_help:
允许主机和容器间利用一个自定义的 (macvlan) 网络直接通信。<br>
默认禁用该选项。
:end

:docker_user_defined_network_active_help:
显示为 Docker 移除或保留在 GUI 之外创建的网络。当设为保留时，在容器的 *网络类型* 下拉列表中 *用户定义网络* 将可用。
:end

:docker_scrub_help:
**清理**运行 *btrfs 清理* 程序以检查文件系统的完整性。

若需要修复，您应当检查 *修正文件系统错误* 并再次运行清理并通过；这将允许  *btrfs 清理* 修复文件系统。
:end

:docker_cancel_help:
**取消** 将会取消正在进行的清理操作。
:end

:id_server_name_help:
服务器的网络标识。也被称作 *主机名* 或 *短主机名* 。Windows 网络
将其指代为 *NetBIOS 名* ，且必须小于等于 15 个字符。
仅可使用字母数字字符（也就是 "A-Z" ， "a-z" ，以及 "0-9" ）,短划线 ("-") ，和点 (".") ；
同时，第一个和最后一个字符必须是字母数字。
:end

:id_description_help:
在网络，或文件资源管理器 (Windows) ，或 Finder (macOS) 中列出服务器时，将会
在旁边显示该文本字段。
:end

:id_model_help:
这是服务器模型数。
:end

:mgmt_start_page_help:
选择在进入 GUI 时首次打开的页面。默认选择 *主磁盘* 页面。
:end

:mgmt_use_telnet_help:
TELNET 访问默认启用。然而， TELNET 是一种不安全的 CLI 访问类型，
强烈建议您使用 SSH 访问作为替代，并禁用 TELNET 访问。
:end

:mgmt_telnet_port_help:
输入 TELNET 端口，默认端口是 23 。
:end

:mgmt_use_ssh_help:
默认启用 SSH 以提供一种安全的 CLI 访问类型。如果秘钥尚未生成并存储在闪存设备的 */config/ssh* 文件夹中，
系统会在启动之后立即自动生成 SSH 秘钥。
:end

:mgmt_ssh_port_help:
输入 SSH 端口，默认端口是 22 。
:end

:mgmt_use_upnp_help:
启用（默认）或禁用服务器的 UPnP 功能。这一功能允许自动转发端口到路由器上，只有当路由器上也已启用 UPnP 时才适用。
:end

:mgmt_use_ssl_tls_help:
确定 webGUI 如何响应 HTTP 和/或 HTTPS 协议。

选择 **否** 以禁用 HTTPS 。

选择 **是** 以启用 HTTPS 并将 HTTP 重定向至 HTTPS 。如果没有预配 Let's Encrypt SSL 证书，
则会自动生成一个自签名 SSL 证书以供使用。

如果你正在或计划使用由 Lime-Technology 预配的 Let's Encrypt SSL 证书，
请选择 **自动** 。在预配证书之前， webGUI 仍将保持 http 模式。
一旦预配了证书， webGUI 将自动切换至 https 模式。此外，
还将启用两个后台进程：

- *updatedns* - 该进程在服务器重启完毕 30s 后启动，并联系 Lime Technology
DNS 服务以注册服务器本地 IP 地址。此后将每 10 分钟唤醒一次以防止
本地 IP 地址再次变化。

- *renewcert* - 该进程在服务器重启完毕 60s 后启动，并联系 Lime Technology
证书更新服务以确定您的 Let's Encrypt SSL 证书是否需要续订。
此后将每 24 小时唤醒一次。如果证书将在30天内到期，会自动
预配一个新的证书并下载到服务器。

备注：在签发了一个 Let's Encrypt SSL 证书后，您可以通过更改该字段为 **是**
来关闭 *updatedns* 和 *newcert*后台程序。

**nginx 证书处理详细信息**

nginx 启动脚本将在 USB 引导闪存中以如下顺序查找 SSL 证书：<br>
`config/ssl/certs/certificate_bundle.pem`<br>
`config/ssl/certs/<server-name>_unraid_bundle.pem`

如果两者中都不存在文件，则会在此处自动创建和存储一个自签名 SSL 证书<br>
`config/ssl/certs/<server-name>_unraid_bundle.pem`<br>

签发的 Let's Encrypt 证书会将证书写入到<br>
`config/ssl/certs/certificate_bundle.pem`<br>

**nginx stapling 支持**

nginx 是否启用 OCSP Staping 取决于使用了下列哪个证书：<br>
`config/ssl/certs/certificate_bundle.pem` => 是<br>
`config/ssl/certs/<server-name>_unraid_bundle.pem` => 否
:end

:mgmt_http_port_help:
输入 HTTP 端口，默认端口是 80 。
:end

:mgmt_https_port_help:
输入 HTTPS 端口，默认端口是 443 。
:end

:mgmt_local_tld_help:
输入本地顶级域名。可以留空。
:end

:mgmt_certificate_expiration_help:
**预配** 可用于从 [Let's Encrypt](https://letsencrypt.org/) 分配一个 *免费* 的 SSL 证书
并随后上传到服务器。备注：**强烈** 推荐在此情况下使用一个静态 IP 地址。

**更新 DNS** 可用于为服务器在 unraid.net 上的 FQDN 手动发起 DNS A 记录的更新。注意
DNS 传播更改可能需要1分钟到几个小时（我们设置的 TTL 为 60 秒）。

备注：如果您的路由器或上游 DNS 服务器已启用了
[DNS 重绑定保护](https://en.wikipedia.org/wiki/DNS_rebinding) ，**预配** 可能会失败。
DNS 重绑定保护阻止 DNS 解析到一个私有 IP 网络范围。DNS 重绑定保护是一种安全特性，
用于接入了带有大量漏洞/不可靠的 "web" 接口的旧设备在内的本地 LAN 。

DNS 重绑定保护的来源之一可能是您的 ISP DNS 服务商。这种情况下可以通过切换到另一个不同的
DNS 服务商来解决问题，例如谷歌的公共 DNS 。

而更普遍的情况下，DNS 重绑定保护可能是由您的路由器启用的。大多数消费级路由器不执行 DNS 重绑定保护；
但如果执行了，应当有一条配置设置可用于关闭它。

然而，较高端的路由器往往会**启用** DNS 重绑定保护。通常可以通过一些方法来基于域名
完全地或选择性地关闭它。例如：

**DD-WRT:** 如果您正在使用启用了 DNS 重绑定保护的 "dnsmasq" ，可以在路由器的配置文件中
添加以下一行：

`rebind-domain-ok=/unraid.net/`

**pfSense:** 如果您正在使用 pfSense internal DNS resolver 服务，您可以添加以下自定义选项行：

`server:`<br>
`private-domain: "unraid.net"`

**Ubiquiti USG router:** 您可以添加以下自定义配置行：

`set service dns forwarding options rebind-domain-ok=/unraid.net/`

**OpenDNS:** 转到 设置 -> 安全 然后*移除*位于 "Suspicious Responses
 - Block internal IP addresses"  旁的复选框。这是一个全有或全无的设置。
 
若以上方法均不奏效，您可以在电脑的 *host* 文件创建一个条目，以覆盖外部 DNS ，
并直接将您服务器的 unraid.net FQDN 重定向到它的本地 IP 地址。
:end

:ftp_server_help:
启用或禁用 FTP 服务器守护程序。FTP 服务器默认启用。
这一设置不会保存，也就是说，一旦系统重新启动，它将会还原回默认设置。
:end

:ftp_users_help:
输入允许使用 FTP 访问服务器的用户名（使用空格分开）。
要禁止任何用户的访问，请清空该项设置。

**注意：**不要输入用户名 `root` ，因为这可能在随后引发某些问题。
:end

:ftp_overview_help:
### 概况

Unraid 包含有广受欢迎的 `vsftpd` FTP 服务器。`vsftp` 的设置现在十分
简单：**所有**在上面输入的用户名都可以通过 FTP 访问服务器，并且拥有
对整个服务器*完全读取/写入/删除的权限*，因此请谨慎使用。
:end

:smb_enable_help:
选择 ‘是（工作组）’以启用 SMB （Windows 网络） 协议支持。这也会
启用 Windows 主机发现。

选择‘是 (Active Directory) ’以启用 Active Directory 集成。
:end

:smb_hide_files_help:
如果设为‘是’，那么以 '.' （句点）开头的文件将会显示为 *隐藏文件* 并且通常
不会出现在 Windows 文件夹列表中，除非您已经在 Windows 文件夹选项中启用了
 “显示隐藏的文件、文件夹和驱动器”。
 
如果设为‘否’，那么句点开头的文件将如同其他文件一样出现在文件夹列表中。
:end

:smb_enhanced_macos_help:
当设置为‘是’时，为 Apple SMB 客户端增强兼容性，体现在诸如更快的 Finder 浏览，
以及导出到 Time Machine 共享的能力。然而，这可能会导致一些 Windows 客户端的问题。
请参阅 [VFS_FRUIT 主页] (https://www.mankier.com/8/vfs_fruit) 。
:end

:smb_enable_netbios_help:
选择‘是’以启用 NEtBios 。如果启用，则可以识别 SMBv1 协议。如果禁用，
客户端必须使用 SMBv2 或更高版本。
:end

:smb_enable_wsd_help:
选择‘是’以启用 WSD （WS 发现）。只有当您正在运行一个基于 SMBv1 的老旧 LAN 设置时，
才需要关闭这一项。
:end

:smb_wsd_options_help:
这是一个命令行选项，设定在启动时输入给 WSD 守护程序的字符串。除非在客户支持的指导下填写，
否则请将此字段留空。
:end

:smb_extra_conf_help:
使用该页面来修改 `smb-extra.conf` 文件。为使更改生效，需要
重新启动 Samba 。
:end

:smb_extra_button_help:
单击 **应用** 按钮以提交当前的编辑。单击 **重置** 以撤销您
作的任何更改（在保存之前）。单击 **完成** 以离开当前页面。
:end

:smb_workgroup_help:
输入您的本地网络工作组名。通常这是 "WORKGROUP" 。
:end

:smb_local_master_help:
如果设为‘是’，那么服务器将会完全参与浏览器选举，并在其他服务器缺席时，
通常会成为本地主浏览器。
:end

:nfs_enable_help:
选择‘是’以启用 NFS 协议。
:end

:nfs_tunable_fuse_remember_help:
当启用 NFS 时，这一可调项可用于减轻或解决您可能在 NFS 客户端上遇到的
"NFS Stale File Handles" 问题。

本质上来说， (fuse_remember) 告知一个内部子系统（名为 "fuse" ）“记住”或“缓存”
与用户共享关联的文件和目录信息多长时间。当一个 NFS 客户端尝试访问服务器上的
一个文件（或目录），且该文件（或目录）没有被缓存时，您可能会遇到
"stale file handle" 。

这一可调项的数值是缓存文件/目录名条目的秒数，
默认值为 330，也就是 5.5 分钟。有两个可以设置的
特殊值：

* 0 意味着完全不缓存文件/目录名，以及
* -1 意味着永久缓存文件/目录名（或者直到阵列停止）

如果您启用了 NFS ，但只计划使用它作为磁盘共享，而非用户共享时，设为 0 值是
较为合适的。

如果没有其他的超时设置能够解决您客户端上的 "stale file handle" 时，设为 -1 值
是较为合适的。需要意识到，设为 -1 值将会导致内存占用情况以缓存每个文件/目录名
占用大约108 byte 的速度增长。这可能会导致内存不足的情况，也可能不会，取决于
服务器安装了多少内存以及您通过 NFS 访问了多少文件/目录。
:end

:shares_enable_disk_help:
如果设为‘否’，那么磁盘共享将不能自由导出。

如果设为‘是’，磁盘共享可能会被导出。**警告：**不要从一个磁盘共享复制数据到用户共享，
除非您 *了解* 您在做什么。这可能会引发数据丢失，并且不会予以支持。

如果设为‘自动’，只有未参与到用户共享的磁盘共享可以被导出。
:end

:shares_enable_shares_help:
如果设为‘是’，将会激活用户共享特性。
:end

:shares_included_disks_help:
该设置定义了在用户共享中 *包括* 的一组阵列磁盘。
取消勾选所有磁盘将会包括 **所有** 阵列磁盘。
:end

:shares_excluded_disks_help:
该设置定义了在用户共享中 *排除* 的一组阵列磁盘。
取消勾选所有磁盘将不会排除任何磁盘

**备注：**每个单独的用户共享都包含有自定的一个包括与排除磁盘的集合，
作为在此处定义的包括/排除磁盘的子集。
:end

:shares_tunable_hard_links_help:
如果设为是，则支持 link() 操作。

如果设为否，则不支持硬链接。

备注：

*如果设为是，当较旧的媒体和 dvd/bluray 播放器使用 NFS 访问共享时可能会产生问题。
*无论该项如何设置， **mover** 都能够正确地操作任何检测到的硬链接。
:end

:shares_tunable_direct_io_help:
**实验性**：如果设为是，则通过 FUSE *direct_io* 挂载选项来挂载用户共享文件系统。
这将会增强写入性能，但有可能会降低读取性能。

*自动* 选择否。
:end

:syslog_local_server_help:
使服务器充当一个中心 syslog 服务器，并从其他系统收集 syslog 信息。
服务器能够监听 UDP ， TCP 或 两者，并选择所需的端口号。

Syslog 信息为每个 IP 地址单独存储。也就是说，每个系统都将拥有一个自己的 syslog 文件。
:end

:syslog_local_folder_help:
选择存储 syslog 的共享文件夹。
推荐使用位于缓存驱动器上的共享，以减少阵列磁盘的旋转。
:end

:syslog_local_rotation_help:
默认禁用日志轮换，并创建单个无限制大小的日志文件。

启用日志轮换可限制日志文件的大小，并指定保存在轮换方案中的文件数。
:end

:syslog_local_file_size_help:
指定最大的日志大小。超出时将会产生日志轮换。
:end

:syslog_local_file_number_help:
指定保存在轮换方案中的文件数。
:end

:syslog_remote_server_help:
输入一个远程 syslog 服务器的名称或 IP 地址。
这将复制 syslog 消息并发送给指定的服务器。
:end

:syslog_mirror_flash_help:
该选项默认设置为否，并且必须谨慎使用以避免不必要地插入和移除 USB 设备。

在需要故障排除，且无法得到常规的诊断信息时，更改这一设置为是。
syslog 文件的镜像存储在闪存设备的 **logs** 文件夹内。
:end

:confirm_reboot_help:
需要通过确认复选框以选择重新启动或者关闭服务。
:end

:confirm_array_stop_help:
需要通过确认复选框以选择是否停止阵列。
:end

:display_settings_help:
以下显示设置确定了各项目如何显示在屏幕上，利用这些设置来调整为你喜爱的视觉效果。

你可以根据需要来试验这些设置，它们只会影响视觉属性。
:end

:display_font_size_help:
此设置针对不同设备独立生效
:end

:display_wwn_device_id_help:
全球通用名称 (WWN) 是一个用于 SAS 连接设备的特定标识符。

选择*已禁用* 将不会在设备标识上追加 WWN

选择*自动* 将会对 SAS 设备的标识追加 WWN
:end

:display_custom_text_color_help:
影响标题中默认的文本颜色。该选项可用于将文本颜色与背景图片进行合理搭配。
:end

:display_custom_background_color_help:
影响标题中默认的背景颜色。该选项可用于将背景颜色与自定义的文本颜色进行合理搭配。
:end

:display_custom_banner_help:
图片将会被缩放到 1920x90 像素。上传图片文件的最大大小为 512 kB (524,288 bytes) 。
:end

:display_temperature_unit_help:
选择磁盘温度阈值的温度单位。更改这一单位将会调整磁盘温度阈值中已设定的值为适合新单位的值。

请确保任何新输入的值对应已选择的温度单位。
:end

:vms_enable_help:
停止虚拟机管理器的操作将会首先尝试关闭所有正在运行中的虚拟机。在 60 秒后，所有剩余的虚拟机实例都会被终止。
:end

:vms_libvirt_volume_help:
这是一个 libvirt 卷。
:end

:vms_libvirt_vdisk_size_help:
该数值为系统在创建一个新的 libvirt 映像文件时默认的文件大小，以 GB 为单位定义。
若要调整一个已有的映像文件的大小，请在此处指定新的值。文件（及文件系统）将会在下一次启动 Libvirt 服务的时候增大到新的大小（但绝不会减小）。
:end

:vms_libvirt_location_help:
必须为 Libvirt 定义一个映像文件。当 Libvirt 服务首次启动时，系统将会自动创建这一文件。
:end

:vms_libvirt_storage_help:
定义一个用户共享，包含所有的虚拟机子目录及虚拟磁盘
:end

:vms_libvirt_iso_storage_help:
定义一个用户共享，包含所有的操作系统安装介质
:end

:vms_virtio_driver_help:
定义虚拟 CD-ROM (ISO) ，包含由 Fedora 项目提供的 VirtIO Windows 驱动。
从此处下载最新的 ISO ：<a href="https://docs.fedoraproject.org/en-US/quick-docs/creating-windows-virtual-machines-using-virtio-drivers/index.html#virtio-win-direct-downloads" target="_blank">fedoraproject.org</a>

在安装 Windows 时，您会到达一个“未发现磁盘设备”的步骤。屏幕上会有一个选项来浏览驱动。
请在菜单中单击“浏览”并定位到附加的 CD-ROM 。此处将会有针对不同版本 Windows 的不同文件夹。
请打开对应您正在安装的 Windows 版本的文件夹，然后选择里面的 AMD64 子文件夹（即使您使用 Intel 平台，也请选择 AMD64）。
您将会看到3个驱动。请全选，并单击下一步，就会出现您分配的虚拟磁盘了。
:end

:vms_network_bridge_help:
选择您希望用于虚拟机的默认网桥的名字，
'virbr0'设置项将会使 libvirt 利用 NAT （网络地址转换）来创建一个虚拟网桥，
并以 DHCP 服务器的形式工作，直接分发 IP 地址给虚拟机。
在网络设置中创建了虚拟网桥之后，将会列出更多的可选项。

备注：你也可以在每个虚拟机的基础设置中定义一个网桥。
:end

:vms_host_shutdown_help:
当关闭服务器时，该选项定义了对正在运行的虚拟机进行的操作。如果选中 *休眠虚拟机* ，
虚拟机将会收到休眠指令（如果支持的话），否则则会尝试关闭虚拟机。
:end

:vms_shutdown_timeout_help:
当关闭服务器时，该选项定义了在进行强制关机之前，等待多少秒以使虚拟机实现 *优雅* 关闭。
备注：推荐在 VM 菜单中内关闭客户虚拟机。
:end

:vms_acs_override_help:
*PCIe ACS 覆盖* 允许各种硬件组件暴露为独立设备。
通常暴露 *下游* 端口已经足够了。
某个硬件组件可能需要设置为 *多功能* 或 *两者* 以实现更多不同的硬件功能。<br>
必须重新启动系统以使这一设置的更改生效。

**警告：使用这一设置项，在特定的硬件配置下可能会导致数据损坏。**
请访问 [Lime Technology 论坛](https://forums.unraid.net/forum/51-vm-engine-kvm) 以获取更多信息。
:end

:vms_vfio_interupts_help:
如果你的系统不支持中断重新映射，则可通过允许不安全的中断，以启用该项。<br>
必须重新启动系统以使这一设置的更改生效。

**警告：使用这一设置项，在特定的硬件配置下可能会导致数据损坏。**
请访问 [Lime Technology 论坛](https://forums.unraid.net/forum/51-vm-engine-kvm) 以获取更多信息。
:end

:vms_libvirt_log_help:
查看 libvirt 日志：<a id="openlog" title="/var/log/libvirt/libvirtd.log" href="#" onclick="openWindow('/webGui/scripts/tail_log&arg1=libvirt/libvirtd.log','_(Libvirt Log)_',600,900);">/var/log/libvirt/libvirtd.log</a>
:end

:vms_scrub_help:
**清理**运行 *btrfs 清理* 程序以检查文件系统的完整性。
若需要修复，您应当检查 *修正文件系统错误* 并再次运行清理并通过；这将允许  *btrfs 清理* 修复文件系统。
:end

:vms_cancel_help:
**取消** 将会取消正在进行的清理操作。
:end

:eth_interface_description_help:
使用这一可选字段以提供关于连接用途的额外信息。
:end

:eth_mac_address_help:
此处为接口的硬件地址。
当标记设为可用时，在此接口上的所有 VLAN 将会共享相通的硬件地址。
:end

:eth_enable_bonding_help:
绑定是一种将多个物理网络接口合并为一个名为 **bond0** 的单个 *被绑定* 接口的特性。
该选项可用于提高连接冗余和/或系统的吞吐量。
支持多种不同的绑定模式（如下），但某些模式需要合适的交换机支持。
:end

:eth_bonding_mode_help:
**模式 0（平衡 - 轮循）**<br>
该模式将从第一个可用的从属接口到最后一个，按顺序传输数据包。
当两个物理接口从属于同一个绑定，且两个数据包的到达目标均为该绑定接口时，第一个数据包奖杯传输给第一个从属接口，而第二帧则会被传输到第二个从属接口。
第三个数据包会被传输到第一个从属接口，如此反复。该模式能提供负载均衡和容错。

**模式 1（主动备份） - 默认**<br>
该模式将其中的一个接口置于备份状态，并且只会在主动接口的链接丢失时激活该接口。
在同一时间实例内，只有一个绑定中的从属接口为活跃状态。只有在活跃的从属接口故障时，才会激活另一个从属接口。
该模式能提供容错。

**模式 2（平衡 - 异或）**<br>
该模式基于异或公式传输数据包。源 MAC 地址与目标 MAC 地址通过异或公式对从属接口数取模。
这将对每个目标 MAC 地址选择相同的从属接口，从而提供负载均衡和容错。

**模式 3（广播）**<br>
该模式在所有从属接口上传输所有数据包。该模式仅偶尔使用（用于特定目的），且只提供容错。

**模式 4（802.3ad）**<br>
该模式被称作*动态链路聚合*。通过创建聚合组来共享相同的速度和双工设置。
需要交换机支持 IEEE 802.3ad 动态链路。
根据选定的传输哈希策略对传出流量的从属接口进行选择，可以通过 xmit_hash_policy 选项来从默认的简单异或策略更改为其他策略。
注意并非所有的传输策略都与 802.3ad 兼容，尤其是 802.3ad 标准的 43.2.4 章节中关于 packet mis-ordering 要求的地方。
不同个例的实现往往有着不同的兼容表现。

**模式 5（平衡 - 传输负载平衡）**<br>
该模式被称为 *自适应传输负载均衡*。传出流量根据现有的每个从属接口的负载和序列情况进行分发。
传入流量由现有的从属接口接收。

**模式 6（平衡 - 自适应负载平衡）**<br>
该模式被称为 *自适应负载均衡* ，包括平衡 - 传输负载平衡和对 IPV4 流量的接收负载平衡 (rlb) 。
接收负载均衡利用 ARP 协商来实现。
绑定的驱动将拦截服务器发出的 ARP 回应报文，并且用绑定中的某一个从属接口的独有硬件地址改写源硬件地址，
使得服务器对不同的客户端使用不同的硬件地址。

*推荐设置为模式 1（主动备份）。其他模式允许您设置特定的环境，但可能需要合适的交换机支持。
选择一个不支持的模式可能导致通信中断。*
:end

:eth_bonding_members_help:
选择哪些接口作为*绑定的*接口的成员。默认情况下 eth0 是成员之一，其他接口为可选项。
:end

:eth_enable_bridging_help:
桥接是一种用于创建虚拟桥并允许虚拟机和 Docker 容器直接通过物理网口通信的特性。
绑定和桥接都能联合使用，以让虚拟机或容器通过一个*绑定的*接口通信。
:end

:eth_bridging_members_help:
选择哪些接口作为*桥接的*接口的成员。默认情况下 eth0 是成员之一，其他接口为可选项。
:end

:eth_network_protocol_help:
选择使用哪个（些）协议。默认使用仅 IPV4 。<br>
当选择 IPV4 和 IPV6 两者时，每个协议都可以独立配置。
:end

:eth_ipv4_address_assignment_help:
可以进行以下设置：

*自动* - 服务器将尝试从本地 DHCP 服务器获取 IPv4 地址 <br>
*静态* - 为此接口手动设置 IPv4 地址 <br>
*无* - 不为接口分配 IPv4 地址（仅适用于 VLAN ）
:end

:eth_ipv4_address_help:
使用自动 IP 分配时显示为灰色。 否则，请在此处指定系统的 IPv4 地址和掩码。
:end

:eth_ipv4_default_gateway_help:
使用自动 IP 分配时显示为灰色。 否则请在此处指定路由器的 IPv4 地址。
:end

:eth_ipv4_dns_server_assignment_help:
如果设置为 *自动*，则服务器将使用本地自动分配返回的 IPv4 DNS 服务器。<br>
如果设置为 *静态*，则可以输入自己的列表。

这在 Active Directory 配置中很有用，在该配置中，您需要将第一个 DNS 服务器条目设置为 AD 域服务器的 IP 地址。
:end

:eth_ipv4_dns_server_help:
这是要使用的主要 IPv4 DNS 服务器。 输入一个 IPv4 地址。

注意：对于 *Active Directory*，您 **必须** 确保将其设置为 AD 域服务器的 IP 地址。
:end

:eth_ipv4_dns_server2_help:
这是 IPv4 DNS 服务器 1 关闭时要使用的 IPv4 DNS 服务器。
:end

:eth_ipv4_dns_server3_help:
这是 IPv4 DNS 服务器 1 和 2 都关闭时要使用的 IPv4 DNS 服务器。
:end

:eth_ipv6_address_assignment_help:
可以进行以下设置：

*自动* - 服务器将尝试从本地 DHCP 服务器或路由器公告（RA）获取 IPv6 地址 <br>
*静态* - 为此接口手动设置 IPv6 地址 <br>
*无* - 不为接口分配 IPv6 地址（仅适用于 VLAN ）
:end

:eth_ipv6_address_help:
使用自动 IP 分配时显示为灰色。 否则请在此处指定系统的 IPv6 地址。
:end

:eth_ipv6_default_gateway_help:
使用自动 IP 分配时显示为灰色。 否则请在此处指定路由器的 IPv6 地址。
:end

:eth_ipv6_privacy_extensions_help:
根据 RFC4941 启用或禁用随机 IPv6 接口标识符的生成。 这类似于 Windows 计算机上的临时 IPv6 地址生成。
:end

:eth_ipv6_dns_server_assignment_help:
如果设置为*自动*，则服务器将使用本地自动分配返回的 IPv6 DNS 服务器。<br>
如果设置为“静态”，则可以输入自己的列表。
:end

:eth_ipv6_dns_server_help:
这是使用的主要 IPv6 DNS 服务器。 输入一个IPv6地址。
:end

:eth_ipv6_dns_server2_help:
这是 IPv6 DNS 服务器 1 关闭时要使用的 IPv6 DNS 服务器。
:end

:eth_ipv6_dns_server3_help:
这是 IPv6 DNS 服务器 1 和 2 都关闭时要使用的 IPv6 DNS 服务器。
:end

:eth_desired_mtu_help:
这是物理以太网接口上使用的 MTU 大小。
如果留空，则将自动确定 MTU （默认为 1500 字节）。
:end

:eth_enable_vlans_help:
默认情况下，未配置任何 VLAN 。<br>
启用 VLAN 可将逻辑连接数扩展到同一物理连接上。

注意：使用此功能时，您的路由器和交换机也必须支持 VLAN 。
:end

:eth_vlan_number_help:
为每个 VLAN 分配一个唯一的标识符。 编号范围是 1 到 4095。
:end

:eth_network_rules_help:
接口分配规则可以在此处更改，并且可能是设置管理 Unraid 的首选接口所必需 - *小心使用，通常无需更改*。

**eth0** 是用于管理 Unraid 系统的主要界面。 其他接口是可选，可以根据需要使用。
每个接口必须通过其 MAC （硬件）地址唯一标识。

接口分配存储在闪存设备上的 */config/network-rules.cfg* 下。 可以使用任何编辑器查看此文件，但是建议仅通过 webGUI 进行更改。<br>
从闪存设备中删除文件 *network-rules.cfg* 将在系统重启后恢复自动接口分配。
:end

:eth_routing_table_help:
输入 *nnn.nnn.nnn.nnn/xx* 格式的有效 IPv4 路由，例如 *192.168.1.0/24* <br>
或以 *nnnn:nnnn:nnnn:nnnn/xxx* 的格式输入有效的 IPv6 路由，例如 *fe80:3ad8:2fff:fe25:9709/64*

从下拉列表中选择网关，或输入有效的 IPv4/IPv6 地址作为网关值。

度量值是可选，默认值为 1。当同一路由存在多个条目时，可使用它来选择首选网关。
:end

:apc_ups_daemon_help:
设置为“是”以启用 apcupsd 并启动子程序，设置为“否”以禁用 apcupsd 并停止子程序。
:end

:apc_ups_cable_help:
定义将UPS连接到计算机的电缆的类型.``电缆''的可能通用选择是：

+ USB ，简单，智能，以太或自定义以指定特殊电缆。
:end

:apc_ups_custom_cable_help:
通过型号指定特殊电缆，仅在 * UPS 电缆*设置为自定义时适用。

+ 940-0119A, 940-0127A, 940-0128A, 940-0020B
+ 940-0020C, 940-0023A, 940-0024B, 940-0024C
+ 940-1524C, 940-0024G, 940-0095A, 940-0095B
+ 940-0095C, 940-0625A, M-04-02-2000
:end

:apc_ups_type_help:
定义一个* UPS 类型*，它与您拥有的 UPS 类型相对应（有关更多详细信息，请参见说明）。

+ **USB** - 大多数新的 UPS 是 USB
+ **APCsmart** - 较新的串行字符设备，适用于使用串行电缆（非 USB ）的 SmartUPS 型号
+ **Net** - 通过 apcupsd 的网络信息服务器连接到主 apcupsd 的网络链接。 如果为计算机供电的UPS连接到另一台计算机进行监视，则使用此方法
+ **SNMP** - SNMP 网络链接到启用 SNMP 的 UPS 设备
+ **Dumb** - 用于简单信号 UPS 的旧串行字符设备
+ **PCnet** - PowerChute 网络关闭协议，可与 AP9617 系列智能插槽卡一起用作 SNMP 的替代协议
+ **ModBus** - 与支持 MODBUS 协议的最新 SmartUPS 型号一起使用的串行设备
:end

:apc_ups_device_help:
输入与您的情况相对应的*设备*，仅在* UPS 类型*未设置为 USB 时适用。

+ **apcsmart** - /dev/tty**
+ **net** - 主机名：端口。 主机名是 NIS 服务器的 IP 地址。 默认端口为 3551
+ **snmp** - 主机名：端口：供应商：社区。 主机名是网络上 UPS 的 IP 地址或主机名。 主机名是网络上 UPS 的 IP 地址或主机名。 "APC_NOTRAP" 将禁用 SNMP 陷阱捕获;  您通常需要 "APC" 。 端口通常是 161 。社区通常是“私有”
+ **dumb** - /dev/tty**
+ **pcnet** - ipaddr：用户名：密码：端口。 ipaddr 是 UPS 管理卡的 IP 地址。 用户名和密码是已配置卡的凭据。 端口是用于侦听来自 UPS 的消息的端口号，通常为 3052。如果此参数为空或缺失，将使用默认值 3052。
+ **modbus** - /dev/tty**
:end

:apc_battery_level_help:
如果在电源故障期间，剩余电池百分比（由 UPS 报告）低于或等于*电池电量*， apcupsd 将启动系统关机。
:end

:apc_runtime_left_help:
如果在电源故障期间，剩余以分钟为单位的运行时间（由 UPS 内部计算）低于或等于此字段， apcupsd 将启动系统关闭。
:end

:apc_battery_time_help:
如果在电源故障期间， UPS 依靠电池运行*超时*数秒或更长时间； apcupsd 将启动系统关闭。 零值将禁用此计时器。

如果您有 Smart UPS ，您最可能希望通过将此计时器设置为零来禁用它。
这样，您的 UPS 将继续使用电池，直到剩余电量百分比降至或低于*电池电量*或剩余电池运行时间降至或低于*分钟*。

当然 - 在测试时 - 如果您拔下电源插头，将其设置为 60 会导致系统快速关闭。
如果您使用的是旧式 UPS，则将其设置为小于您可以使用电池供电的时间。
:end

:apc_note_help:
**注意：** *电池电量*，*剩余运行时间*和*电池续航时间*一起工作，因此以上任何一项最先发生将启动关机。
:end

:apc_killups_help:
设置为*是*以在关机后关闭UPS的电源。
:end

:parity_check_scheduled_help:
默认情况下不计划会奇偶校验检查。 在此处选择所需的计划。 这可以是每日，每周，每月，每年或自定义计划。
:end

:parity_day_of_the_week_help:
当选择了**每周**或**自定义**计划后，请在此处选择首选的*周中一日*，在其他计划中，此设置将不被使用且不可用。
:end

:parity_week_of_the_month_help:
当选择**每月**或**每年**计划后，请在此处选择首选的*月中一日*。
当选择了**自定义**计划后，请在此处选择首选的*月中一周*，在其他计划中，此设置将不被使用且不可用。
:end

:parity_time_of_the_day_help:
选择所需的*日中时间*以开始计划。 时间间隔以半小时为单位。
:end

:parity_month_of_the_year_help:
当选择了**年度**或**自定义**计划后，请在此处选择首选的*年中一月*，在其他计划中，此设置将不被使用且不可用。
:end

:parity_write_corrections_help:
在此处选择是否需要在奇偶校验磁盘上更正检查期间发现的任何奇偶校验错误。
:end

:mover_schedule_help:
选择每小时，每天，每周和每月的 Mover 计划。

间隔确定 Mover 将在后台运行的速度。
:end

:mover_day_of_the_week_help:
选择每周计划后选择一天。 否则将禁用。
:end

:mover_day_of_the_month_help:
选择每月计划后选择一个日期。 否则将禁用。
:end

:mover_time_of_the_day_help:
选择按小时计划后，将以小时为单位设置间隔。 间隔始终始于整点（0分钟）。

对于其他计划，请在此处选择 Mover 应该在一天中的开始时间。
:end

:mover_logging_help:
将 Mover 讯息写入系统日志文件。
:end

:notifications_display_help:
在*详细*视图中，所有通知会立即显示在屏幕上。<br>
通知可以单独确认，也可以一次全部确认。

在*摘要*视图中，通知将仅计算未读通知数并显示在每个类别的菜单标题中。<br>
单击计数器以确认或查看未读的通知。
:end

:notifications_date_format_help:
选择在通知存档中所使用的日期格式。 推荐使用年—月—日，这样可使日期／时间列以合理的方式排序。
:end

:notifications_time_format_help:
选择在通知存档中所使用的时间格式。 推荐使用24小时制，这样可使日期／时间列以合理的方式排序。
:end

:notifications_display_position_help:
选择通知在屏幕上出现的位置。 多个通知会堆叠在一起，从下到上或从上到下，具体取决于所选的展示位置。
:end

:notifications_store_flash_help:
默认情况下，通知将存储在RAM磁盘上，在系统重新引导时这些通知将丢失。
通知可被永久存储在闪存驱动器的文件夹 '/boot/config/plugins/dynamix' 下.
:end

:notifications_system_help:
默认情况下，通知系统是禁用的。 在此处启用它以开始接收通知。
以下各节提供了有关发送哪种类型的通知的更多选项。
:end

:notifications_os_update_help:
启动定期验证，并在检测到新版本的Unraid OS系统时通知用户。
使用下面的复选框选择如何发出通知； 通过浏览器，通过电子邮件 和/或 通过自定义代理。
:end

:notifications_plugins_update_help:
启动定期验证，并在检测到一个或多个已安装插件的新版本时通知用户。
使用下面的复选框选择如何发出通知； 通过浏览器，通过电子邮件 和/或 通过自定义代理。
:end

:notifications_docker_update_help:
启动定期验证，并在检测到一个或多个已安装 Docker 的新版本时通知用户。
使用下面的复选框选择如何发出通知； 通过浏览器，通过电子邮件 和/或 通过自定义代理。
:end

:notifications_array_status_help:
启动定期阵列运行状况检查（预防性维护），并将检查结果通知用户。
:end

:notifications_agent_selection_help:
使用上面的复选框选择需要发出通知的内容和方式； 通过浏览器，通过电子邮件 和/或 通过服务。<br>
<b>　提示：</b> 您可以使用自定义通知代理； 只需将它们添加到 "/boot/config/plugins/dynamix/notification/agents" 目录并选中“代理”即可.
:end

:notifications_classification_help:
通知分类为：

*注意* - 这些是资讯性的通知，并不表示出现问题情况，例如有新版本可用<br>
*警告* - 这些是需要注意的通知，可能表示将来的问题，例如硬盘比平时更热<br>
*警报* - 这些是严重的通知，需要立即关注，例如发生故障的硬盘<br>

为每个分类选择要如何被通知。
:end

:smtp_preset_service_help:
选择一个预设服务以设置基本服务设置。
:end

:smtp_email_address_help:
您的邮件帐户的电子邮件地址。 该地址用作通知的发送者。
:end

:smtp_recipients_help:
状态和错误通知的接收者。 指定一个或多个电子邮件地址，用空格分隔多个电子邮件地址。
:end

:smtp_priority_help:
当 unRaid 出现问题时，请将电子邮件标头设置为高度重要。
:end

:smtp_subject_prefix_help:
设置前缀以便识别 Unraid 消息。
:end

:smtp_mail_server_help:
指定电子邮件服务器的名称。 使用预设服务选择来自动填写此内容。
:end

:smtp_mail_server_port_help:
指定电子邮件服务器的端口。 使用预设服务选择来自动填写此内容。
:end

:smtp_use_ssl_tls_help:
指定是否使用 SSL / TLS 与 SMTP 服务器对话。
:end

:smtp_use_starttls_help:
指定在开始 SSL 协商之前是否使用 STARTTLS - 请参阅 RFC 2487。
:end

:smtp_define_tls_cert_help:
仅当您具有通信所需的证书时才选择。
:end

:smtp_tls_cert_location_help:
用于 TLS 的 RSA 证书的文件名 - 是需要的。
:end

:smtp_authentication_method_help:
为您的电子邮件服务器选择正确的身份验证方法。 使用测试来验证访问是否正常运作。
:end

:smtp_username_password_help:
输入用户名和密码以登录到您的电子邮件帐户。 请注意，存储在电子邮件配置文件中的密码并未加密。
:end

:plugin_install_help:
要下载并安装插件，请输入 plg 文件网址，然后单击**安装**。 這将打开一个窗口显示安装进度。
在安装完成之前，请勿关闭此窗口。 您也可以指定扩展名的本地文件名。
:end

:plugin_error_help:
由于某种安装错误，未安装这些插件。 您应该删除这些插件，然后**重启**服务器。*
:end

:plugin_stale_help:
由于已经存在更新的代码，因此未安装这些插件。 只需将删除它们就可。
:end

:docker_client_general_help:
模板是在Unraid服务器上设置 Docker 容器的更快方法。 有两种类型的模板：

**默认模板**<br>
将有效的存储库添加到 Docker Repositories 页面后，它们将显示在此下拉列表中的一部分供您选择（母版按作者分类，然后按应用程序模板分类）。
选择默认模板后，该页面将在“描述”字段中写入有关该应用程序的新信息，并且通常会提供有关如何设置容器的说明。
首次配置此应用程序时，请选择一个默认模板。

**用户定义模板**<br>
通过默认模板将应用程序添加到系统后，
您指定的设置将保存到USB闪存设备中，以便在升级失败或发生其他问题时轻松地重建应用程序。
如要重建，只需从“用户定义”列表中选择先前加载的应用程序，容器的所有设置将显示为先前设置中的状态。
单击创建将重新下载该应用程序所需的文件，并恢复到可运作状态。
要删除用户定义模板，请从上面的列表中选择，然后单击它右边的红色 X。
:end

:docker_client_name_help:
为容器命名或保留默认名称。
:end

:docker_client_overview_help:
应用程序容器的描述。 支持基本的 HTML 标记。
:end

:docker_client_repository_help:
Docker Registry 上应用程序的存储库。 格式为作者名 / 应用名。
（可选）您可以在应用名之后添加 '：' ，并请求容器映像的特定版本。
:end

:docker_client_support_thread_help:
链接到 Lime-Technology 论坛上的支持主题。
:end

:docker_client_project_page_help:
链接到项目页面（例如： www.plex.tv ）
:end

:docker_client_donation_text_help:
在“应用”标签中的“捐赠链接”上显示的文字
:end

:docker_client_donation_link_help:
链接到捐赠页面。 如果使用捐赠，必须设置映像和链接
:end

:docker_client_hub_url_help:
容器在Docker Hub上的存储库位置的路径。
:end

:docker_client_template_url_help:
此URL用于保持模板更新。
:end

:docker_client_icon_url_help:
链接到您的应用程序的图标映像（如果 “显示设置” 下的 “显示仪表板应用程序” 设置为 “图标” ，则仅显示在仪表板上）。
:end

:docker_client_webui_help:
当您从 Docker 容器页面单击应用程序图标时，WebUI 选项将链接到此字段中的路径。
使用 [IP] 标识主机的 IP ，然后使用 [PORT：####] 替换端口的 ＃。
:end

:docker_extra_parameters_help:
如果您希望在运行时将其他命令附加到 Docker 容器，可以在此处指定它们<br>
有关所有可能的 Docker 运行时命令，请参见此处： <a href="https://docs.docker.com/reference/run/" target="_blank">https://docs.docker.com/reference/run/</a>
:end

:docker_post_arguments_help:
如果希望在容器定义后附加其他参数，可以在此处指定。
字段的内容是特定于容器。
:end

:docker_cpu_pinning_help:
检查一个或多个 CPU 内核将限制该容器仅在选定的内核上运行。 如果不选择核心，容器将在所有可用核心上运行（默认）
:end

:docker_fixed_ip_help:
如果选择网桥类型，应用程序的网络访问将被限制为仅在端口映射部分中指定的端口上进行通信。
如果选择主机类型，将为应用程序提供访问权限，使其可以使用主机上尚未映射到另一个正在使用的应用程序 / 服务的任何端口进行通信。
一般来说，建议将此设置保留为每个应用程序模板指定的默认值。

重要说明：如果要调整端口映射，请不要修改容器端口的设置，因为只能调整主机端口。
:end

:docker_privileged_help:
对于需要直接使用主机设备访问权限或需要充分利用主机功能的容器，将需要选择此选项。
有关更多信息，请参见以下链接： <a href="https://docs.docker.com/engine/reference/run/#runtime-privilege-and-linux-capabilities" target="_blank">https://docs.docker.com/engine/reference/run/#runtime-privilege-and-linux-capabilities</a>
:end

; sysdevs help - added May 18, 2020
:sysdevs_iommu_groups_help:
此将显示系统上可用的 IOMMU 组的列表，以及每个 IOMMU 组的 lspci 命令的输出。 数字标识符用于配置PCI直通。

您选择的设备将在引导时绑定到 vfio-pci 驱动程序，这使它们可用于分配给虚拟机，并且还防止 Linux 内核自动将它们绑定到任何现有的主机驱动程序。

**请注意，选择设备不仅会绑定指定的设备，而且还会绑定同一 IOMMU 组中的*所有*其他设备。**

&nbsp;<i class="fa fa-circle orb green-orb middle"></i>&nbsp;此符号表示设备当前已绑定到 vfio-pci 驱动程序。

&nbsp;<i class="fa fa-retweet grey-orb middle"></i>&nbsp;&nbsp;&nbsp; 此符号表示设备支持 FLR （功能级别重置）。

<input type="checkbox" value="" disabled>&nbsp;&nbsp; 如果复选框为灰色，则表示该设备已被 Unraid 使用，无法直通。
:end

:sysdevs_thread_pairings_help:
此显示CPU线程配对的列表。
:end

:sysdevs_usb_devices_help:
此显示 "lsusb" 命令的输出。 数字标识符用于配置PCI直通。
:end

:sysdevs_scsi_devices_help:
此显示 "lsscsi" 命令的输出。 数字标识符用于配置PCI直通。

请注意, Linux 将 ATA, SATA 和 SAS 设备与真正的 SCSI 设备进行分组。
:end
